/**
 * ZKTeco Attendance — Global JS
 * Included in every desk page via hooks.py app_include_js
 */

// Register custom navbar link / desk shortcut
frappe.after_ajax(() => {
	// No-op for now — workspace handles navigation
});
